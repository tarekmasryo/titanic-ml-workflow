# Artifacts

This folder is created/used by the notebook and scripts to store outputs such as:

- trained model (`model.joblib`)
- run metrics (`metrics.json`)
- configuration and mappings (`metadata.json`)

Artifacts are not committed by default.
